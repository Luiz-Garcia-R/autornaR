% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rnaUpset.R
\name{rna.upset}
\alias{rna.upset}
\title{Generate an UpSet plot of expressed genes across groups}
\usage{
rna.upset(comp_data, group_col = "Group", cutoff = 5, results = FALSE)
}
\arguments{
\item{comp_data}{A \code{normalized_data} object (with DESeq2 comparisons), an \code{rnaCompare} object, or a \code{DESeqDataSet}.}

\item{group_col}{Character; the metadata column defining groups (default \code{"Group"}).}

\item{cutoff}{Numeric; expression threshold to consider a gene as expressed (default \code{5}).}

\item{results}{Logical; if TRUE, returns lists of genes by group, unique genes, and shared genes (default \code{FALSE}).}
}
\value{
Invisibly returns gene lists if \code{results = TRUE}; otherwise prints the UpSet plot.
}
\description{
Produces an UpSet plot showing the overlap of expressed genes across experimental groups.
Expression is considered present if normalized counts exceed the specified cutoff.
}
