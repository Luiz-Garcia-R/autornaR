% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rnaRank.R
\name{rna.rank}
\alias{rna.rank}
\title{Rank genes by mean or variance}
\usage{
rna.rank(
  norm_data,
  group_col = "Group",
  metric = c("mean", "var"),
  top_n = NULL,
  organism = "mouse",
  highlight_gene = NULL,
  plot = TRUE
)
}
\arguments{
\item{norm_data}{Object returned by \code{\link[=rna.normalize]{rna.normalize()}} containing \code{expr_matrix} and \code{metadata}.}

\item{group_col}{Character; column in metadata defining sample groups (default \code{"Group"}).}

\item{metric}{Character; either \code{"mean"} or \code{"var"} to rank by mean expression or variance (default \code{"mean"}).}

\item{top_n}{Integer; number of top genes to retain per group (default \code{NULL}, keeps all genes).}

\item{highlight_gene}{Character vector of gene symbols or Ensembl IDs to highlight in the plot (default \code{NULL}).}

\item{plot}{Logical; whether to plot the ranking (default \code{TRUE}).}

\item{species}{Character; \code{"human"}, \code{"mouse"} or \code{"zebrafish"} for gene annotation (default \code{"mouse"}).}
}
\value{
A list containing:
\describe{
\item{ranked_genes}{Data frame with statistics and rank per gene per group.}
\item{plot}{ggplot2 object of gene rankings.}
}
}
\description{
Rank genes based on expression mean or variance within two groups of samples.
Optionally highlight specific genes and plot rankings per group.
}
