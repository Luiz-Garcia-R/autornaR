% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rnaTtest.R
\name{rna.ttest}
\alias{rna.ttest}
\title{Perform t-tests or Mann-Whitney tests for selected genes}
\usage{
rna.ttest(
  normalized_data,
  genes,
  group_col = "Group",
  species = "human",
  utest = FALSE,
  return_type = c("htest", "all")
)
}
\arguments{
\item{normalized_data}{Object of class \code{normalized_data} (from \code{rna.normalize()}).}

\item{genes}{Character vector of gene symbols or Ensembl IDs to analyze.}

\item{group_col}{Character; metadata column defining sample groups (default \code{"Group"}).}

\item{species}{Character; \code{"human"} or \code{"mouse"} for gene annotation (default \code{"human"}).}

\item{utest}{Logical; whether to use Mann-Whitney test instead of t-test (default \code{FALSE}).}

\item{return_type}{Character; \code{"htest"} returns only test results, \code{"all"} returns results and plots (default \code{"all"}).}
}
\value{
List of t-test (or Mann-Whitney) results and plots, depending on \code{return_type}.
}
\description{
Performs statistical comparison between two groups for a set of genes using t-test (default)
or Mann-Whitney test (utest = TRUE), and plots boxplots with significance annotation.
}
