% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rnaIdentify.R
\name{rna.identify}
\alias{rna.identify}
\title{Identify and classify RNA types from normalized RNA-seq data}
\usage{
rna.identify(
  normalized_data,
  group_col = "Group",
  organism = "mouse",
  top_n_types = 5,
  plot = TRUE,
  lnc_only = FALSE
)
}
\arguments{
\item{normalized_data}{A \code{normalized_data} object returned by \code{rna_normalize()},
containing an expression matrix (\code{expr_matrix}) and associated metadata.}

\item{group_col}{Character. Column name in metadata specifying the sample groups
(default: \code{"Group"}).}

\item{top_n_types}{Integer. Maximum number of RNA biotypes displayed individually
in the summary plot; remaining types are grouped as \code{"Other"} (default: \code{5}).}

\item{plot}{Logical. Whether to display a stacked barplot summarizing RNA types
per group (default: \code{TRUE}).}

\item{lnc_only}{Logical. If \code{TRUE}, filters and classifies only lncRNAs
(default: \code{FALSE}).}

\item{species}{Character. Either \code{"human"}, \code{"mouse"} or \code{"zebrafish"} to define the Ensembl dataset
(default: \code{"mouse"}).}
}
\value{
A list of class \code{"rnaIdentify"} containing:
\item{rna_lists}{A named list of gene identifiers by RNA biotype.}
\item{plot_bar}{A \code{ggplot} object of the RNA biotype distribution (if \code{plot = TRUE}).}
\item{info}{A data frame with Ensembl annotations, gene types, and lncRNA subclassification.}
\item{summary_by_group}{A data frame summarizing RNA biotype proportions per group.}
}
\description{
This function uses Ensembl BioMart to annotate gene biotypes and classify
expressed transcripts (including lncRNAs) in normalized RNA-seq data. It can
optionally generate a summary plot of RNA type proportions per group.
}
\details{
The function connects to Ensembl via \pkg{biomaRt} to retrieve gene biotypes and
additional metadata. It supports human, mouse and zebra fish datasets.
When \code{lnc_only = TRUE}, only long non-coding RNAs (lncRNAs) are kept and subclassified
into categories (e.g., \emph{Antisense}, \emph{Intergenic}, \emph{Processed transcript}).
}
\examples{
\dontrun{
result <- rna.identify(normalized_data, group_col = "Condition")
result_lnc <- rna.identify(normalized_data, lnc_only = TRUE)
}

}
